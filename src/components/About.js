import React from "react";
import { Element } from "react-scroll";

const About = props => {
	return (
		<Element className="section-container" name="about-section">
			About
		</Element>
	);
};

export default About;
